#!/bin/sh

# gtk-config: wrapper around pkg-config for backward compatibility
# with scripts using gtk-config
#
# Benoit Laniel <nels@pgroupe.net>

if test "$1" = "--version"; then
  ARGS="--modversion"
else
  ARGS=$1
fi

pkg-config gtk+-1.3-win32-production $ARGS